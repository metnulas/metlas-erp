import Link from "next/link";
import { ArrowLeft, Edit, Mail, Phone, UserRound } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { Personnel } from "@prisma/client";
const labels: Record<string, string> = { ACTIVE: "Aktif", ON_LEAVE: "İzinli", INACTIVE: "Pasif" }; const styles: Record<string, string> = { ACTIVE: "bg-emerald-50 text-emerald-700", ON_LEAVE: "bg-amber-50 text-amber-700", INACTIVE: "bg-muted text-muted-foreground" };
export default function PersonnelDetail({ personnel }: { personnel: Personnel }) { return <div className="space-y-6"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="flex min-w-0 items-center gap-3"><Button variant="ghost" size="icon" render={<Link href="/personnel" />}><ArrowLeft className="size-4" /></Button><div className="min-w-0"><h1 className="truncate text-2xl font-bold tracking-tight">{personnel.fullName}</h1><p className="font-mono text-xs text-muted-foreground">{personnel.employeeCode}</p></div></div><Button className="w-full md:w-auto" variant="outline" render={<Link href={`/personnel/${personnel.id}/edit`} />}><Edit className="size-4" /> Düzenle</Button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Durum" value={labels[personnel.status] ?? personnel.status} tone={styles[personnel.status]} /><Stat label="Görev" value={personnel.position} /><Stat label="Telefon" value={personnel.phone} /><Stat label="Ehliyet" value={personnel.licenseClass || "Yok"} /></div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-xl border border-border/70 bg-card p-6"><h2 className="mb-5 flex items-center gap-2 text-lg font-semibold"><UserRound className="size-5 text-primary" /> İletişim Bilgileri</h2><div className="space-y-4"><Info icon={<Phone className="size-4" />} label="Telefon" value={personnel.phone} /><Info icon={<Mail className="size-4" />} label="E-posta" value={personnel.email} /></div></section><section className="rounded-xl border border-border/70 bg-card p-6"><h2 className="mb-5 text-lg font-semibold">İş ve Ehliyet Bilgileri</h2><div className="grid gap-4 sm:grid-cols-2"><Info label="Ehliyet No" value={personnel.licenseNumber} /><Info label="Ehliyet Geçerlilik" value={personnel.licenseExpiryDate ? new Date(personnel.licenseExpiryDate).toLocaleDateString("tr-TR") : null} /><Info label="İşe Giriş" value={personnel.hireDate ? new Date(personnel.hireDate).toLocaleDateString("tr-TR") : null} /></div></section></div>{personnel.notes && <section className="rounded-xl border border-border/70 bg-card p-6"><h2 className="mb-3 text-lg font-semibold">Notlar</h2><p className="whitespace-pre-wrap text-sm text-muted-foreground">{personnel.notes}</p></section>}</div>; }
function Stat({ label, value, tone }: { label: string; value: string; tone?: string }) { return <div className="rounded-xl border border-border/70 bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className={`mt-2 inline-flex rounded-full px-2 py-1 text-base font-bold ${tone ?? ""}`}>{value}</p></div>; }
function Info({ icon, label, value }: { icon?: React.ReactNode; label: string; value?: string | null }) { return <div className="flex items-center gap-3">{icon && <span className="text-muted-foreground">{icon}</span>}<div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-1 text-sm font-semibold">{value || "-"}</p></div></div>; }
